# Passport_OCR_paddle_ocr
Passport OCR module using PaddleOCR to extract raw text from passport images. Designed for reliable text extraction with minimal preprocessing and easy integration with regex or LLM-based structured data pipelines.
